@using Microsoft.AspNetCore.Identity
@model LogisticCompany_Identity.ViewModel.UserWithRole

@inject RoleManager<IdentityRole> roleManager
@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<h1>Edit</h1>

<h4>Car</h4>
<hr/>
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div>
                <select name="role" asp-items="@(new SelectList(roleManager.Roles))" ></select>
            </div>

            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary"/>
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>